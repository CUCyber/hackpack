\section{Rsyslog}

Rsyslog is a client and server that conforms to the syslog protocol.
Systems that use systemd, often use journald instead.

\acmlisting[label=rsyslog,caption=rsyslog]{./logging/syslog/scripts/syslog.sh}

